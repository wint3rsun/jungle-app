<div class="card text-center w-50 mx-auto">
  <div class="card-body">
    <h5 class="card-title"><%= category.name %></h5>
    <%= link_to "See all #{category.name}", category, class: "btn btn-primary" %>
  </div>
</div>